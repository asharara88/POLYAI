# Agencies

Owned by `agency-liaison`. One folder per external agency:
- `agencies/<agency-slug>/profile.md` — agency profile, SLA, primary contacts, performance log
- `agencies/<agency-slug>/engagements/<engagement-id>/` — per-campaign engagement records

Skip for clients with no external agency engagements.
