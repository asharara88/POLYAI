# Brokers

Owned by `broker-enablement`. Files:
- `registry.md` — broker network registry (firms, individuals, tier, specialization, performance)
- `routing-rules.md` (optional) — client-specific overrides on top of `verticals/<vertical>/playbook.md` routing principles

Only used for clients with an external broker / channel sales motion. Skip for direct-sale clients.
